<div class="container mx-auto " style="width: 1000px;">
  <p id="notice"></p>

      <h4>新着商品</h4>
      <div class="row">

         <% @items.each do |item| %>
         <div class="col-md-3">
              <%= link_to item_path(item.id) do %>
              <%= image_tag item.image, size: "120x90"%><br>
              <% end %>
              <%= item.name %><br>
              <%= item.price %>円
        </div>
          　 <% end %>

　    </div>
</div>